<Page
    x:Class="HungerKiller.publish"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HungerKiller"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="100"></RowDefinition>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>
        <Image Name="ChoosenPhotoImage" MaxHeight="80" MinWidth="60" VerticalAlignment="Center" ></Image>
        <Button Name="AddPhoto"
                Height="50" Width="50"
                Content="插入图片"
                Background="Lavender" 
                Click="AddPhoto_Click" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Right" ></Button>

        <TextBox MinHeight=" 300" MinWidth="400" Grid.Row="2" ></TextBox>
        
        <Button Content="发布" FontSize="40" 
                Grid.Row="3"
                HorizontalAlignment="Right" 
                VerticalAlignment="Center"
                Background="Lavender" ></Button>
    </Grid>
</Page>
