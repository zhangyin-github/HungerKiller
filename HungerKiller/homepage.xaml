<Page
    x:Class="HungerKiller.homepage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HungerKiller"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:HungerKiller.Model"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" RenderTransformOrigin="0.505,0.439">
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions  >
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!--新品上市轮播控件-->
        <Grid HorizontalAlignment="Center" Grid.Row="0" Margin="0,5,0,5" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!--左-->
            <Grid Grid.Column="0" Margin="0,20,0,20">
                <Grid.Background>
                    <SolidColorBrush Color="LightGray"/>
                </Grid.Background>
                <FlipView x:Name="LeftFlipView" Opacity="0.5" IsEnabled="False" >
                    <FlipView.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding}" Stretch="Fill"/>
                        </DataTemplate>
                    </FlipView.ItemTemplate>
                </FlipView>
            </Grid>

            <!--中-->
            <FlipView x:Name="CenterFlipView" Grid.Column="1">
                <FlipView.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding}" Stretch="Fill"/>
                    </DataTemplate>
                </FlipView.ItemTemplate>
            </FlipView>

            <!--右-->
            <Grid Grid.Column="2" Margin="0,20,0,20">
                <Grid.Background>
                    <SolidColorBrush Color="LightGray"/>
                </Grid.Background>
                <FlipView x:Name="RightFlipView" Opacity="0.3" IsEnabled="False" >
                    <FlipView.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding}" Stretch="Fill"/>
                        </DataTemplate>
                    </FlipView.ItemTemplate>
                </FlipView>
            </Grid>
            
        </Grid>

        <StackPanel  Name="juece" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                     Tapped="juece_Tapped" BorderBrush="LightGray" BorderThickness="0,0,1,0" Orientation="Horizontal">
            <Image Source ="Assets/1.jpg" Width=" 50" Height=" 50" HorizontalAlignment="Center"/>
            <TextBlock Text="决策" HorizontalAlignment="Center"/>
            
        </StackPanel>

        <StackPanel  Name="tuijian" Grid.Row="1" Grid.Column="1" Tapped="tuijian_Tapped" BorderBrush="LightGray" BorderThickness="0,0,1,0">
            <Image Source ="Assets/1.jpg" Width=" 50" Height=" 50" HorizontalAlignment="Center" Margin=" 0,25,0,0"/>
            <TextBlock Text="推荐" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel  Name="paihang" Grid.Row="1" Grid.Column="2" Tapped="paihang_Tapped">
            <Image Source ="Assets/1.jpg" Width=" 50" Height=" 50" HorizontalAlignment="Center" Margin=" 0,25,0,0"/>
            <TextBlock Text="排行" HorizontalAlignment="Center"/>
        </StackPanel>

        <SplitView Name="MySplitView"
                       Grid.Row="2"
                       DisplayMode="Inline"
                       OpenPaneLength="150"
                       CompactPaneLength="45"
                       Grid.ColumnSpan="3">

            <SplitView.Content >
                <GridView Name="NewsItemGrid"
                          ItemsSource="{x:Bind NewsItems}"
                          HorizontalAlignment="Stretch"
                          Margin="34,10,-24,-10">
                    <GridView.ItemTemplate >
                        <DataTemplate x:DataType="data:NewsItem">
                            <local:NewsItemsControl/>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
